# Source

Place production code in this directory. Prefer feature- or domain-based
subdirectories over grouping every file only by technical type.

Example once the product modules are known:

```text
src/
├── features/
├── shared/
└── entrypoint
```

Replace this guide with the real source layout when implementation begins.


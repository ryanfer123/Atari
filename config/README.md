# Configuration

Store version-controlled application, tooling, and deployment configuration in
this directory. Commit safe defaults and examples only; keep credentials and
developer-specific values in ignored environment files.

Model selection and bounded generation defaults live in `model/`. Model weights,
tokens, credentials, and developer-specific filesystem paths do not belong here.

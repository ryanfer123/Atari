# Tests

Place automated tests and reusable test support here. Keep the layout aligned
with the source boundaries where practical.

Suggested categories when the test suite grows:

- `unit/` for isolated behavior
- `integration/` for component boundaries
- `e2e/` for complete user or system flows
- `fixtures/` for stable test data

